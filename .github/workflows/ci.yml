name: CI

on: 
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: [macOS-latest]

    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: pod install
    - name: Compile
      run: fastlane ci
